const NotFound = () => {
    // Page for if user navigates to page that doesn't exist. In future should contain a redirect for user but they can use Navbar for now
    return (<h1>Not Found</h1>)
}
export default NotFound;