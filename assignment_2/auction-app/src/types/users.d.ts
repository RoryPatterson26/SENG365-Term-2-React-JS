type User = {
    /**
     * All the components of a user type
     */
    firstName: string,
    lastName: string,
    email: string,
    password: string,
    //Profile image
}

type userReturnWithEmail = {
    firstName: string,
    lastName: string,
    email:string
}